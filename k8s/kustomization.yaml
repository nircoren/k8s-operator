apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hello-app

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

labels:
  - pairs:
      project: hello-k8s-demo
    includeSelectors: false
